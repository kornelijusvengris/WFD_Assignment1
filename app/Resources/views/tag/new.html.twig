{% extends '_base.html.twig' %}

{% block body %}
    <h1>Tag creation</h1>

    {{ form_start(form) }}

    <div class="form-field">
        {{ form_row(form.content) }}
        {{ form_row(form.votes) }}
    </div>

    <div>
        <button><a href="{{ path('tag_index') }}">Back to the List</a></button> | <input type="submit" value="Create" />
    </div>

    {{ form_end(form) }}

{% endblock %}
