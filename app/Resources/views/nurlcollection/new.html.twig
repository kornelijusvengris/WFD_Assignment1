{% extends '_base.html.twig' %}

{% block body %}
    <h1>Nurlcollection creation</h1>

    {{ form_start(form) }}

    <div class="form-field">
        {{ form_row(form.title) }}
        {{ form_row(form.owned_by) }}
    </div>

    <div>
        <button><a href="{{ path('nurlcollection_index') }}">Back to the List</a></button> | <input type="submit" value="Create" />
    </div>

    {{ form_end(form) }}
{% endblock %}
