{% extends '_base.html.twig' %}

{% block body %}
    <h1>Pendingnurl creation</h1>

    {{ form_start(form) }}

    <div class="form-field">
        {{ form_row(form.approved) }}
        {{ form_row(form.nurl) }}
    </div>

    <div>
        <button><a href="{{ path('pendingnurl_index') }}">Back to the List</a></button> | <input type="submit" value="Create" />
    </div>

    {{ form_end(form) }}

{% endblock %}
