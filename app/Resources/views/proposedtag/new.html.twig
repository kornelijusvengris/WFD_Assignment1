{% extends '_base.html.twig' %}

{% block body %}
    <h1>Proposedtag creation</h1>

    {{ form_start(form) }}

    <div class="form-field">
        {{ form_row(form.title) }}
        {{ form_row(form.votes) }}
    </div>

    <div>
        <button><a href="{{ path('proposedtag_index') }}">Back to the List</a></button> | <input type="submit" value="Create" />
    </div>

    {{ form_end(form) }}

{% endblock %}
